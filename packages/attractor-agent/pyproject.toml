[project]
name = "attractor-agent"
version = "0.1.0"
description = "Coding agent loop primitives"
requires-python = ">=3.12"
dependencies = [
    "attractor-llm",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/attractor_agent"]
