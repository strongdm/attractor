[project]
name = "attractor-llm"
version = "0.1.0"
description = "Unified multi-provider LLM client"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/attractor_llm"]
