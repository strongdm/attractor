[project]
name = "attractor"
version = "0.1.0"
description = "Graph execution engine for Attractor"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/attractor"]
